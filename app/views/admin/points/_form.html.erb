<%= form_for([:admin,@point]) do |f| %>
  <% if @point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
      <% @point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :participant_id %><br />
    <%= f.select :participant_id, options_for_select(@participants.collect{|p| [p.name,p.id]}.sort,@point.participant_id) %>
  </div>
  <div class="field">
    <%= f.label :point_type_id %><br />
    <%= f.select :point_type_id, options_for_select(@point_types.collect{|pt| [pt.name,pt.id]}.sort,@point.point_type_id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
