<%= form_for([:admin,@point_type]) do |f| %>
  <% if @point_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@point_type.errors.count, "error") %> prohibited this point_type from being saved:</h2>

      <ul>
      <% @point_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :value, "Points" %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
