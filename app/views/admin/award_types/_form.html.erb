<%= form_for([:admin,@award_type]) do |f| %>
  <% if @award_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@award_type.errors.count, "error") %> prohibited this award_type from being saved:</h2>

      <ul>
      <% @award_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :num_points %><br />
    <%= f.text_field :num_points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
