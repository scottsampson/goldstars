<%= form_for([:admin,@participant]) do |f| %>
  <% if @participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant.errors.count, "error") %> prohibited this participant from being saved:</h2>

      <ul>
      <% @participant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :role_id %><br />
    <%= f.select :role_id, options_for_select(@roles.collect{|role| [role.role_name,role.id]}.sort) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
